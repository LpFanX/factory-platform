# Платформа (обёртка). Движок content-agents тянет свои зависимости отдельно (PyYAML).
fastapi>=0.110
uvicorn[standard]>=0.29
PyYAML>=6.0
openai>=1.40          # клиент OpenAI-совместимого шлюза (AITunnel) для реальных прогонов
